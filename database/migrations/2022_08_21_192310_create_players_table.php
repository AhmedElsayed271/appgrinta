<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

class CreatePlayersTable extends Migration
{
    /**
     * Run the migrations.
     *
     * @return void
     */
    public function up()
    {
        Schema::create('players', function (Blueprint $table) {
            $table->id();
            $table->string('current_value');
            $table->enum('position',['Goalkeeper','Centre-Backs','Full-Backs','Wing-Backs','Defensive Midfielders','Central Midfielders','Attacking Midfielders','Wingers','Inside Forwards','Strikers']);
            $table->string('twitter_account');
            $table->string('image')->default('default.png');
            $table->bigInteger('team_id')->unsigned();
            $table->foreign('team_id')->references('id')->on('teams')->onDelete('cascade');
            $table->timestamps();
        });
    }

    /**
     * Reverse the migrations.
     *
     * @return void
     */
    public function down()
    {
        Schema::dropIfExists('players');
    }
}
